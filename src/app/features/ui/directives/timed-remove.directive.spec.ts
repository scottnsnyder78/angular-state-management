import { TimedRemoveDirective } from './timed-remove.directive';

describe('TimedRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new TimedRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
